import { FloatPointPipe } from './float-point.pipe';

describe('FloatPointPipe', () => {
  it('create an instance', () => {
    const pipe = new FloatPointPipe();
    expect(pipe).toBeTruthy();
  });
});
